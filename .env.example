# KidzGem Headless WordPress Backend Configuration
VITE_WP_API_URL=https://kidzgem.com
VITE_ENABLE_WP_BACKEND=true

# Resend (server-side only, used by /api/subscribe and /api/contact serverless
# functions - do NOT prefix with VITE_ or it will be bundled into client JS)
RESEND_API_KEY=
RESEND_FROM_EMAIL=KidzGem <onboarding@resend.dev>
# TEMPORARY test recipient - swap for the real store inbox before launch
STORE_NOTIFY_EMAIL=khushiyadavchb@gmail.com

# Supabase (server-side only - the service_role key bypasses Row Level
# Security, so it must NEVER be prefixed with VITE_ or used in client code)
SUPABASE_URL=https://rmpnxnkxsxltntpwrkoq.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
